<h1> This is the listing page</h1>

<p>=====================================</p>
<p> This is listing without yours</p>
<p>=====================================</p>

<% @other_listings.each do |y| %>

  Name: <%= link_to "#{y.name}", listing_path(y), method: :get %> <br>
  Description: <%= y.description %><br>
  City: <%= y.city %><br>
  By: <%= y.user.first_name %><br>
  Tags: <% y.tags.each do |c| %>
	<%= link_to "#{c.name}", "/listings/tags/" + c.id.to_s, methos: :get %>
	<% end %><br><br>
	<%= link_to "Reserve now", new_listing_reservation_path(y) %>
	<p>___________________________________________</p>
 

<% end %>







<br>
<p>====================================</p>
<p>This is all the listing including yours</p>
<p>====================================</p>
<% @listings.each do |x| %>

  Name: <%= link_to "#{x.name}", listing_path(x), method: :get %> <br>
  Description: <%= x.description %><br>
  City: <%= x.city %><br>
  By: <%= x.user.first_name %><br>
  Tags: <% x.tags.each do |b| %>
	<%= link_to "#{b.name}", "/listings/tags/" + b.id.to_s, methos: :get %>
	<% end %><br>
	<p>___________________________________________</p>
 

<% end %>