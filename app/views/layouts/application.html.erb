<!DOCTYPE html>
<html>
<head>
<title>Pair BnB</title>
  <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => "reload" %>  
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tag %>

</head>
<body>
<div class="container">
  <nav class="navbar navbar-default">
    <div class="navbar-header">
      <a class="navbar-brand" href="/">Pair BnB</a>

      <ul class="nav navbar-nav navbar-right">
        <% if signed_in? %>
          <li><%= link_to " " + t(".sign_out"), sign_out_path, method: :delete, class: "glyphicon glyphicon-log-out"%>
          <li><%= link_to " My Profile", user_path(current_user), method: :get, class: "glyphicon glyphicon-user"%>
          <li>Welcome <%= current_user.first_name %>
        <% else %>
          <li><%= link_to " " + t(".sign_in"), sign_in_path, class: "glyphicon glyphicon-log-in"%>
        <% end %>
      </ul>

      <div id="flash">
        <% flash.each do |key, value| -%>
          <div id="flash_<%= key %>">
            <%=h value %>
          </div>
        <% end %>
      </div>
    </div>
  </nav>


  <%= yield %>
</div>

</body>
</html>
