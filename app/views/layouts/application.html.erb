<!DOCTYPE html>
<html>
<head>
<%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => "reload" %>  
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tag %>

</head>
<body>
<div class="container">
  <nav class="navbar navbar-default">

    <div id="header">
      <% if signed_in? %>
        <span>Welcome <%= current_user[:first_name] %>!</span>
        <span><%= button_to t(".sign_out"), sign_out_path, method: :delete %></span>
        <span><%= button_to "My Profile", user_path(current_user), method: :get %></span>
        <span><%= button_to "Home", root_path, method: :get %>
      <% else -%>
        <%= link_to t(".sign_in"), sign_in_path %>
      <% end %>
    </div>

    <div id="flash">
      <% flash.each do |key, value| -%>
        <div id="flash_<%= key %>"><%=h value %></div>
      <% end %>
    </div>

  </nav>

  <%= yield %>
</div>

</body>
</html>
